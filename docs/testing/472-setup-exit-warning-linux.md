# Setup exit warning — affected Linux native validation

## Exact candidate and safety boundary

Validate the current #555 correction and its exact debug binary after independent
source review and focused/precommit gates. Use a fresh unconsumed namespace,
`/tmp/horizon-472-setup-exit-warning-ui-smoke`, derived mechanically from the
reviewed RunPod disclosure preview kit. Preserve all older campaigns and receipts.
No Create click, consent check, cloud/provider access, remote-store/key seeding,
Docker container, repository execution, real credentials or user config changes.
Only synthetic profiles and two empty task-owned local owning sessions are seeded
through the existing public SessionStore fixture. Keep network isolation and
PID/start-time/window checks, terminal-complete trace checks and original bounded
20-minute supervisor deadlines. Never replay consumed bootstrap or launch intent.

## Deterministic coverage

Retain all 85 focused UI tests: consent, single-flight, owner/config/home drift,
lost replies, typed pre-delivery refusal versus ambiguous outcomes, original
coordinates, noncreating Check and interrupted/saved-only observations.
The existing pending timing regression must show the exit-interruption warning
at 89, 90, 179 and 180 seconds, preserving its original no-action assertions.
The existing LocalDocker/RunPod review regression must retain every prior field
assertion and show the same warning. No test claims controller-process survival.

## Dark phase — form and review disclosure

1. Bootstrap once, launch the exact isolated binary on an unused display at
   1200x900 and capture the baseline/actual PID/deadline. No remote record exists.
2. Open New from the empty board. The blank form must state that closing the
   overview only detaches its view, exiting Horizon may interrupt local setup,
   an existing attempt may still bill, and the user should Check the original
   saved workspace instead of resubmitting. Capture and inspect this warning.
3. Select the configured synthetic RunPod profile using the inline selector;
   fill the same admissible explicit image/repository/commit/branch/argv/storage
   values as the reviewed no-cloud fixture. Review remains filesystem/provider
   free. Inspect the warning, complete profile/HPS values and exact owner.
4. Scroll to expiry/consent. The wording refers to a *created* persistent worker;
   it does not promise an in-flight local setup thread survives process exit.
   Consent stays unchecked and Create disabled. Never click either control.
5. Resize to the 800x600 native minimum. Capture readable form/review warning,
   long values and reachable consent with scrolling; restore, Cancel and verify
   a fresh form has no retained consent. Keep empty-board Fit qualified as an
   inert disabled control, not populated-canvas fit proof.
6. Separate idle and pointer observations must add no operational work. Close
   normally before the original deadline. Verify exact process/display absence,
   complete traces, absent remote store/keys and zero provider/repository dispatch.

## Light phase and completion

After the first normal close and hash/identity checks, change only the owned
config theme to light. Use a fresh unused display and the same untouched local
fixture. Repeat actual blank-form and populated-review warning captures, narrow
scrolling, created-worker/unchecked-consent text, Cancel, idle/pointer controls
and normal close. Do not change optional profile values or add acceptance lanes.

Root independently inspects decisive images and final hash-bound evidence.
Pending-operation warning rendering is deterministic test coverage, not a native
creation experiment. Historical full LocalDocker settled-setup/task survival is
historical; this lane proves only current warning visibility and no-I/O behavior.
No durable external setup executor or shutdown-completion guarantee is added.
Archive this temporary plan privately and remove its repository copy after PASS.
