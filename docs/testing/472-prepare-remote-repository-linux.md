# Linux repository preparation — typed refusal regression

Temporary validation plan for #544. Do not treat old native results as proof of
the revised error handling. The reviewed implementation checkpoint is cc4f376b;
bind the final integrated source only after #546 actually merges. Preserve the
primary checkout, all existing desktops/processes and consumed fixture evidence.

## Preconditions and boundaries

- Root reviews the complete source diff, helper delta and final bindings before
  build/runtime. Required focused tests and blocking/strict Clippy must pass.
- Use a fresh isolated Linux GUI and one task-owned LocalDocker worker. No real
  PAT, real GitHub request, cloud allocation, image publication or user config.
- Reuse the locally pinned worker image and installed helper/CLI provenance,
  independently verifying hashes. Build fresh exact-source GUI and fixture
  driver under the shared cache lock with dependency parity and no upgrades.
- Use fixed synthetic HTTPS Git upload-pack routes inside the worker, a synthetic
  CA/token and unchanged production commands. Only loopback SSH is published.
- The saved target Shell stays never started. A separate finite scratch task
  supplies one-start, advancing-ticks and zero-attached-clients evidence.
- Current private execution kit: `/tmp/horizon-472-repository-preparation-ui-smoke-v3`.
  Its PLAN and helpers specify exact ownership, size/deadline and no-replay gates.
  This plan is also executable manually with equivalent isolated resources and
  the same recorded assertions; do not reuse consumed intents or fixture state.

## Required cases

1. Capture baseline at1200x900 and literal repository/commit/branch/destination
   disclosure. Cancel does no work. Empty/invalid token or absent explicit
   consent disables confirmation; cancel/reopen/undo cannot restore plaintext.
2. Create a fresh InstallFirst preview. Withdraw only the exact synthetic client
   private key AFTER preview and BEFORE confirmation, retaining its inode/hash in
   a private journalled location. Never regenerate or overwrite a key or pin.
3. Enter the synthetic token, consent and Confirm once. Expect a definite identity
   refusal, cleared form, no new unknown/possibly-installed warning and no retry.
   Verify zero SSH install/submit/status/other commands and zero HTTP requests;
   no token/pending token, checkout/claim/receipt or target task exists. The same
   scratch task advances, unattached, and saved allocation/pin remain unchanged.
4. Restore the exact same key once. Make a NEW preview/consent and confirm once.
   Observe one real pinned installation and one detached Git handoff. Submitted
   is not Complete. Manual Check reaches original Complete/no-reason and exact
   commit/work branch/tracked bytes with authenticated synthetic HTTP evidence.
5. After known completion, explicitly confirm a different synthetic token once.
   Refuse rotation conservatively, preserve old token inode/hash and Git state,
   and issue no new Git submission. Do not infer rollback from a failure.
6. Dirty a tracked file and add an untracked sentinel. Manual Check, same-row
   selection, idle/pointer and resize/scroll/focus/Fit preserve both and add no
   unsolicited operations. Capture readable1200x900 and960x720 screenshots.
   Qualify actual Fit interaction; do not claim every panel is visible.
7. Close normally, then verify scratch progress without a client. Reopen the same
   home in a new isolated display: no install/submit replay; manual Check still
   reports original completion, not current task readiness. Close normally again.
8. Stop only the exact fixture server through its normal path. Root independently
   reviews screenshots, traces, source/binary/input hashes and normal-exit proofs.
   Then guarded cleanup removes only the recorded new fixture container while
   preserving baseline resources and private homes/receipts.

## Deterministic and failure evidence

The typed UI table covers definite local/state/consent/config/recovery refusals,
nested Git/Credential and OutcomeUnknown ambiguity, current/stale scope and
sticky prior unknown state. Existing tests retain lost replies, discarded actual
mutation results, single-flight and same/invalid/changed-selection behavior.
The actual app refusal test covers both Prepare and Inspect with no dispatch.
Native timing does not substitute for these deterministic callback-race tests.

If GUI supervision fails while the key is held, restore only its recorded inode
and hash after owned child cleanup; fail closed if restoration is uncertain.
Preserve failed intents and do not retry setup, install or Git automatically.
No broad process signals, worker task cancellation, claim repair or deletion of
preexisting resources. Record omitted/unproved real-provider/platform cases.

After successful native review/cleanup, archive and delete this temporary plan,
prove production/Cargo parity, and run one final exact-head full matrix before
push. Refresh independent hosted review and current-head CI. Squash and postmerge
workflow verification retain their exact-head gates; releases are not authorized.
