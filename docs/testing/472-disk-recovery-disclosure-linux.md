# Local Docker disk disclosure and recovery feedback smoke

## Candidate and boundaries

Validate the exact reviewed debug candidate for the two PR #555 corrections.
The form and review must call disk a requested value, explicitly not enforced by
Local Docker. Wrong-home/session Check must report how to recover, without
dispatch or replacement of original coordinates. No backend quota is added.

Use a fresh private namespace derived from the reviewed preview-only native kit.
Do not reuse consumed intents, homes, display processes, binaries or receipts.
Bind the frozen source, Cargo identities, helpers and new copied debug binaries.
Build and runtime remain unarmed until their respective independent reviews.

## Isolation and setup

- Use two synthetic empty persistent owners and a synthetic Local Docker profile.
- No saved remote allocation, private key, token or provider credentials may be
  seeded. Do not connect Docker, create a container or start any remote service.
- Run in the existing isolated network-namespace pattern; use an empty private
  home and explicit child environment. Preserve all pre-existing host processes.
- Verify an unused task-owned virtual display and scope every input to the exact
  candidate PID/start-time/window. Retain unique intents and trace evidence.
- Keep each supervisor's original 20-minute deadline. Close normally before it;
  record any incomplete check honestly rather than extending or replaying.

## Dark phase

1. Capture the baseline and open New with no profile automatically selected.
   Check the empty-board controls and truthful absent-store presentation.
2. Select the synthetic Local Docker profile through the actual inline selector.
   Capture the requested-disk label and its explicit unenforced qualification.
3. Enter public synthetic digest, positive disk value, repository, exact commit,
   work branch, nested directory and literal argv. Review once.
4. Capture full review values, disk qualifier, existing interruption/no-resubmit
   warning and created-worker qualifier. Consent must remain unchecked and
   Create disabled. Never toggle consent or click Create.
5. Resize to 800x600; scroll form/review as needed and capture readable qualifier,
   long values and reachable Cancel/unchecked consent. Restore normal geometry.
6. Record idle and pointer movement separately: no provider/repository execution,
   network connection, remote store/key creation or automatic preview replay.
7. Cancel, reopen a fresh unselected form and verify no consent/review survives.
   Close normally and verify exact child/display absence and complete no-I/O trace.

## Light phase

After dark normal close, change only the owned config theme under hash/metadata
guards. Use a new display and unique launch intent. Repeat form, review, 800x600
scrolling, unchecked consent, Cancel, separate idle/pointer and normal-close proof.
Do not add optional profile mutations or claim a populated-canvas Fit result for
the empty-board fixture.

## Deterministic recovery and regression gates

The existing real state/action/render regression covers wrong home and wrong
owner independently: reset the prior notice, invoke Check, assert the exact new
notice, no pending work, unchanged locator and sticky uncertainty; render the
notice alongside inventory controls. This is not native retained-history proof.
Preserve all 85 focused cases and all existing exact provider-field assertions.
Run focused tests, fmt/maintainability/version, blocking and strict Clippy before
commit. After independent native review, archive/remove this temporary plan,
prove source/Cargo parity and run the full exact-final-head validation matrix.
Retain historical earlier native evidence as historical, not current-head proof.
