# RunPod exact consent disclosure — Linux affected smoke

## Candidate and boundaries

Validate the corrected #555 checkout and exact debug binary, never an older
binary. This is a new, unconsumed no-cloud preview/cancel campaign in
/tmp/horizon-472-runpod-disclosure-ui-smoke. Preserve the historical actual
LocalDocker Create → Prepare → Start → reconnect proof; it is not current-head
RunPod creation evidence. No container, cloud resource, PAT, key, real registry
credential, provider socket or image pull is allowed. Never check consent or
click Create.

Reuse the reviewed Azure preview kit's bounded commands, stable metadata,
network-isolated bwrap launch, PID/start-time/window binding, no-overwrite images,
terminal-complete trace checks and 20-minute normal-close supervision. Bootstrap
only two empty local owning sessions and synthetic profile/config using public
SessionStore APIs; never seed a remote store or target. Local session/config
persistence is expected; no-I/O means no remote-state/provider/repository dispatch.

## Preconditions and deterministic coverage

Root reviews source, helper delta and exact build/runtime bindings before launch.
Use offline dependency identities and the shared Cargo lock. Bind all helper and
binary hashes, candidate HEAD/tree and source parity. Use a fresh unused X11
display; no existing session, desktop, process or user config is touched.
Existing focused tests retain consent, scope drift, lost replies, single flight,
sticky uncertainty, default selection and inline-selector coverage. The extended
render test checks every configured RunPod value. All original tests remain.

## First phase — dark, exact configured values

1. Launch at 1200x900; verify empty board and no selected profile. Open New and
   capture blank fields. Incomplete Review refuses locally. Check selected setup
   remains disabled without a selected remote record; New obeys pending/form gates.
2. Select the synthetic RunPod profile via the real inline selector. Enter the
   fixed image, exact commit, branch, nested cwd, literal argv, integer cents,
   HPS volume/data center/minimum size. No credentials are entered.
3. Review and inspect exact GPU choices/count, CUDA allowlist, profile data center,
   ports, profile volume, download/upload floors, raw GraphQL minDisk and the full
   synthetic registry registration ID. No configured/absent placeholder replaces
   a supplied value. Profile volume is GB and unused with HPS; profile DC is marked
   overridden by HPS, while the selected HPS volume/DC/minimum size remain exact.
   A configured nonempty profile DC must match HPS admission; do not invent an
   admitted mismatch. Empty-profile-DC semantics remain explicit Not set.
4. Inspect the actual destination, literal command, owner and continuing-billing
   disclosure. Confirm unchecked consent and disabled Create. Cancel, reopen and
   verify no retained consent or automatic creation; repeat fresh Review only.
5. Exercise invalid zero/fraction/overflow price, all local refusal. Resize to
   native minimum 800x600, scroll long values and consent into view, keyboard
   traverse, restore and invoke actual Fit. Judge new form/overview readability,
   not preexisting canvas minimap overlap.
6. Switch to the other empty local owner with a review open; confirm old preview
   and consent disappear. Record idle/pointer controls with unchanged work counts.
7. Close normally before the existing deadline. Terminal trace must reject partial
   lines/unresolved calls and show no provider exec/INET/repository work. Prove
   remote store/key paths absent and exact GUI/WM/display exits.

## Second phase — light, same local fixture

After normal close, hash-check the owned config, change only its theme to light,
and reopen on a new unused display. No setup is automatic. Repeat full long-value
and narrow consent scrolling, unchecked Cancel, idle/pointer controls and normal
close. Root inspects decisive images and final evidence before completion.
No resource cleanup applies beyond the supervisor's exact task-created groups.
Keep truthful absent-store failure/retry presentation; never seed a store to hide it.
Preserve failed intents; do not replay bootstrap or extend a missed deadline.
Archive this plan privately and remove this temporary repository copy after smoke.
